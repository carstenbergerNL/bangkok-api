# Bangkok API base URL (no trailing slash). Used by the frontend for all API requests.
VITE_API_BASE_URL=https://localhost:56028

# Optional. Default application ID sent with login (if your API requires it). Get the ID from your API (e.g. Default app) and set it here. Leave unset to use a placeholder.
# VITE_DEFAULT_APPLICATION_ID=

# If login fails with "Cannot reach the API": ensure the API is running, and that the API's CORS AllowedOrigins (appsettings Cors section) includes your frontend origin, e.g. http://localhost:5173
