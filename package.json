{
  "name": "bangkok",
  "private": true,
  "description": "Bangkok: API and web app (React + Vite)",
  "workspaces": [
    "app"
  ],
  "scripts": {
    "build:app": "npm run build -w app",
    "dev:app": "npm run dev -w app"
  },
  "engines": {
    "node": ">=18"
  }
}
